# fly.toml app configuration file generated for realtourpilot-production-team on 2026-04-30T18:15:33Z
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'realtourpilot-production-team'
primary_region = 'dfw'

[build]

[env]
  DB_PATH = '/data/realtourpilot.db'
  PORT = '3000'

[[mounts]]
  source = 'realtourpilot_data'
  destination = '/data'

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0

  [[http_service.checks]]
    interval = '30s'
    timeout = '5s'
    grace_period = '10s'
    method = 'GET'
    path = '/api/stats'

[[vm]]
  memory = '1gb'
  cpu_kind = 'shared'
  cpus = 1
  memory_mb = 1024
